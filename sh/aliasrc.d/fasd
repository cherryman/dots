if have fasd; then

    case "${SHELL##*/}" in
        zsh)    arg='zsh-hook'   ;;
        bash)   arg='bash-hook'  ;;
        *)      arg='posix-hook' ;;
    esac

    eval "$(fasd --init $arg)"
    unset arg

    z()  { cd "$(fasd -d "$@")"; }
    zz() { cd "$(fasd -d -i "$@")"; }

fi
