xmodmap "$HOME/.Xmodmap"

case "$(uname -n)" in
    CherryPC)
        xset m 1 0 # Disable mouse accel
        ;;
    CherryLT)
        (sleep 0.5; polybar example) &
        ;;
esac

redshift -t6500:3600 &
sxhkd &
