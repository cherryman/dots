### Programs
alt + shift + e
	rofi -show run

## Screenshot
# selection
alt + shift + s
	maim -s | xclip -i -sel c -t image/png

# select window
alt + shift + w
	maim -i "$(xdotool getactivewindow)" | xclip -i -sel c -t image/png

# everything
Print
	maim | xclip -i -sel c -t image/png

super + grave
	alacritty || xterm

alt + shift + l
	keymap

### XF86 Keys
XF86Audio{Prev,Next,Play}
	playerctl {previous,next,play-pause}

XF86Audio{LowerVolume,RaiseVolume,Mute}
	volume {down,up,mute}

XF86MonBrightness{Up,Down}
	backlight {up,down}

XF86HomePage
	firefox

XF86Explorer
	pcmanfm

# vim: noet
